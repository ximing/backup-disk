## Codebase Patterns
- Project structure follows standard Go layout: cmd/, pkg/, internal/
- CLI commands use cobra framework, defined in cmd/cloudsync/
- Configuration package in pkg/config/ handles YAML parsing and validation
- Logger package in pkg/logger/ provides structured logging with task-specific outputs
- Environment variables in config are expanded using os.ExpandEnv()
- Config directory: ~/.cloudsync/, logs: ~/.cloudsync/logs/, data: ~/.cloudsync/data/

## 2024-02-22T23:15:00+08:00 - US-001
- Implemented Go project structure with cmd/, pkg/config/, pkg/logger/, internal/ directories
- Created go.mod with cobra and yaml.v3 dependencies
- Implemented `cloudsync init` command that generates default config at ~/.cloudsync/config.yaml
- Implemented `cloudsync validate` command that validates YAML syntax and configuration
- Created directory structure: ~/.cloudsync/logs/ and ~/.cloudsync/data/
- Files changed:
  - go.mod
  - cmd/cloudsync/main.go
  - cmd/cloudsync/root.go
  - cmd/cloudsync/init.go
  - cmd/cloudsync/validate.go
  - pkg/config/config.go
  - pkg/config/default.go
  - pkg/logger/logger.go
- **Learnings for future iterations:**
  - Configuration uses environment variable expansion: ${VAR_NAME} syntax
  - Storage backends must implement the Storage interface (to be defined in US-002)
  - Tasks require cron expression parsing (robfig/cron/v3 will be added in US-004)
  - Code uses standard Go project layout with clear separation between cmd/ and pkg/

## 2026-02-22T23:45:00+08:00 - US-002
- Defined Storage interface with Upload, List, Delete, Validate methods
- Implemented S3Storage using AWS SDK v2
- Created factory function NewStorage() for creating storage instances based on config
- Implemented credential validation using ListBuckets API call
- Added error conversion layer to map AWS errors to domain-specific StorageError types
- Updated go.mod with AWS SDK v2 dependencies
- Files changed:
  - go.mod (added AWS SDK dependencies)
  - go.sum (new file with all dependency checksums)
  - pkg/storage/interface.go (Storage interface and ObjectInfo struct)
  - pkg/storage/s3.go (S3Storage implementation)
  - pkg/storage/factory.go (storage factory function)
  - prd.json (marked US-002 as complete)
- **Learnings for future iterations:**
  - Storage interface is located in pkg/storage/interface.go - all new backends must implement it
  - S3Storage uses AWS SDK v2 with support for custom endpoints (S3-compatible services like MinIO)
  - Environment variables are already expanded in config before being passed to storage constructors
  - Error handling uses smithyError interface to extract AWS error codes
  - OSS storage placeholder is in factory.go - will be implemented in US-003
  - Build requires GOPROXY to be set in this environment (use goproxy.cn for faster downloads)

## 2026-02-22T23:55:00+08:00 - US-003
- Implemented OSSStorage using aliyun-oss-go-sdk
- Created OSSConfig struct with endpoint, bucket, access_key_id, access_key_secret fields
- Implemented Upload, List, Delete, Validate methods matching S3Storage interface
- Updated factory.go to create OSS storage instances from config
- Credential validation uses ListBuckets API and bucket ListObjects with max 1 key
- Added error conversion for OSS-specific error patterns
- Files changed:
  - go.mod (added aliyun-oss-go-sdk dependency)
  - go.sum (updated with new dependency checksums)
  - pkg/storage/oss.go (new file with OSS implementation)
  - pkg/storage/factory.go (updated NewOSSStorageFromConfig function)
  - prd.json (marked US-003 as complete)
- **Learnings for future iterations:**
  - OSS SDK uses different method naming than AWS SDK (no GetBucketInfo/GetBucketACL)
  - ListBuckets + ListObjects with MaxKeys(1) is the recommended validation approach
  - OSS SDK error messages contain patterns like "NoSuchKey", "AccessDenied" in error strings
  - Both S3 and OSS storage backends now share the same Storage interface

## 2026-02-23T00:05:00+08:00 - US-004
- Integrated robfig/cron/v3 for task scheduling
- Created pkg/scheduler package with TaskScheduler, Task, and TaskConfig types
- Implemented cron expression validation supporting standard 5-field format and presets (@daily, @hourly, etc.)
- Added utility functions: GetNextRuns(), FormatDuration(), DescribeSchedule()
- Created cron.go with preset definitions and schedule description helpers
- Updated config validation to validate cron schedules during config load
- TaskScheduler supports AddTask, RemoveTask, GetTask, GetAllTasks, Start, Stop operations
- Avoided import cycle by defining local TaskConfig struct in scheduler package
- Files changed:
  - go.mod (added robfig/cron/v3 dependency)
  - go.sum (updated with new dependency checksums)
  - pkg/scheduler/scheduler.go (new file with scheduler implementation)
  - pkg/scheduler/cron.go (new file with preset definitions)
  - pkg/config/config.go (added cron import and schedule validation)
  - prd.json (marked US-004 as complete)
- **Learnings for future iterations:**
  - scheduler package defines its own TaskConfig to avoid import cycle with config package
  - robfig/cron/v3 supports both 5-field standard cron and preset descriptors (@daily, @hourly)
  - Parser is stored separately in TaskScheduler since cron.Cron doesn't expose its parser
  - Config validation now catches invalid cron expressions early
  - Next step: US-005 (compression module) is independent of scheduler

## 2026-02-22T00:20:00+08:00 - US-005
- Implemented compression module with Compressor interface
- Created gzip compressor using Go standard library (compress/gzip)
- Created zstd compressor using github.com/klauspost/compress/zstd
- Implemented compression level support (1-9) for both formats
- Added file filtering: min_size threshold, include_extensions whitelist, exclude_extensions blacklist
- Created NoopCompressor for when compression is disabled
- Compression extensions: .gz for gzip, .zst for zstd
- Files changed:
  - pkg/compress/interface.go (Compressor interface and Config struct)
  - pkg/compress/gzip.go (GzipCompressor with streaming compression)
  - pkg/compress/zstd.go (ZstdCompressor with level mapping)
  - go.mod (added klauspost/compress v1.18.4)
  - go.sum (updated with new checksums)
  - prd.json (marked US-005 as complete)
- **Learnings for future iterations:**
  - Compression uses io.Pipe() for streaming to avoid loading entire files in memory
  - Extension matching is case-insensitive (strings.ToLower used)
  - zstd levels map: 1-2 -> SpeedFastest, 3-5 -> SpeedDefault, 6-8 -> SpeedBetterCompression, 9 -> SpeedBestCompression
  - NoopCompressor returns empty extension when compression is disabled
  - Build requires GOPROXY=https://goproxy.cn in this environment

## 2026-02-23T00:35:00+08:00 - US-006
- Implemented retention policy management package
- Created RetentionManager with Policy struct (MaxDays, MaxVersions)
- Implemented backup listing from cloud storage with date parsing
- Date parsing extracts timestamp from path format: {prefix}/{YYYY}/{MM}/{DD}/{HHmmss}/
- Implemented cleanup logic supporting both max_days and max_versions policies
- Added dry-run mode to preview deletions without actually deleting
- Created comprehensive tests for all retention scenarios
- Files changed:
  - pkg/retention/retention.go (new file with retention manager)
  - pkg/retention/retention_test.go (comprehensive tests)
  - pkg/retention/CLAUDE.md (documentation for the package)
  - prd.json (marked US-006 as complete)
- **Learnings for future iterations:**
  - Backup times are parsed from directory paths, not object modification times
  - Retention manager groups objects by backup directory (based on YYYY/MM/DD/HHmmss path components)
  - Policy evaluation: max_days is applied first, then max_versions on remaining backups
  - Cleanup deletes all objects under a backup directory recursively
  - All times are handled in UTC to avoid timezone issues
  - Next step: US-007 (manual sync command) depends on storage, compression, and retention modules

## 2026-02-23T01:30:00+08:00 - US-008
- Implemented daemon mode with `cloudsync daemon start/stop/status` commands
- Created pkg/daemon package with PID file management, signal handling, and graceful shutdown
- Daemon supports foreground mode (`--foreground` flag) for debugging
- Implemented SIGTERM/SIGINT handling for graceful exit (completes current tasks)
- Implemented SIGHUP handling for configuration reload without restart
- Daemon registers all configured tasks with the scheduler on startup
- Task execution handler integrates with existing sync.Executor
- PID file stored at ~/.cloudsync/data/cloudsync.pid
- Added GetLogDir() and GetDataDir() helper functions to config package
- Files changed:
  - pkg/daemon/daemon.go (new file with daemon lifecycle management)
  - cmd/cloudsync/daemon.go (new file with CLI commands)
  - cmd/cloudsync/root.go (added daemon subcommand)
  - pkg/config/config.go (added GetLogDir and GetDataDir functions)
  - prd.json (marked US-008 as complete)
- **Learnings for future iterations:**
  - Used `gosync` and `syncpkg` aliases to avoid package name conflicts with standard library `sync`
  - Daemon uses fork()+setsid() for Unix daemonization, redirecting stdio to /dev/null
  - Signal 0 can be used to check if a process exists without affecting it
  - Scheduler's Stop() method waits for current jobs to complete before returning
  - Configuration reload creates a new scheduler instance to avoid stopping during task execution
  - When daemonizing, must open log file before redirecting stdout/stderr to /dev/null
  - Next step: US-009 (logging and status query) will need to integrate with daemon's task execution

## 2026-02-23T01:00:00+08:00 - US-007
- Implemented manual sync command `cloudsync sync <task-name>` and `cloudsync sync --all`
- Created pkg/scanner package for file system scanning with include/exclude glob patterns
- Created pkg/sync package to orchestrate scan → compress → upload workflow
- Source scanner supports glob patterns with directory wildcards (e.g., "dir/**", "*.log")
- Implemented progress tracking: files processed, bytes uploaded, duration
- Support for --dry-run mode to preview changes without uploading
- Concurrent uploads limited by semaphore (5 concurrent by default)
- Comprehensive sync statistics: success/failed/skip counts, total bytes
- Failed files recorded and reported at end of sync
- Files changed:
  - cmd/cloudsync/root.go (added sync subcommand)
  - cmd/cloudsync/sync.go (new file with sync CLI implementation)
  - pkg/scanner/scanner.go (new file with file scanning logic)
  - pkg/sync/sync.go (new file with sync execution logic)
  - prd.json (marked US-007 as complete)
- **Learnings for future iterations:**
  - Scanner uses filepath.WalkDir for efficient directory traversal
  - Pattern matching supports both file paths and base names for flexibility
  - Sync execution uses goroutines with semaphore for controlled concurrency
  - Date format in target path follows Go time layout (default: 2006/01/02/150405)
  - Upload flow: open file → compress if needed → upload to {prefix}/{date}/{filename}
  - Next step: US-008 (daemon mode) depends on scheduler and sync modules
---
